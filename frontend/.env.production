# This file is committed to Git and used ONLY during production builds (e.g., Render).
# It provides fallback values if Render env vars are not explicitly set.
# The VITE_API_URL env var set in render.yaml will OVERRIDE this value at build time.
# Left empty so Render's dynamic host injection takes over.
VITE_API_URL=
